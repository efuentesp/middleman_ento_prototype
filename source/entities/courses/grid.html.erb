---
title: Cursos en línea - eLearning
---
<%= partial 'partials/page_heading' %>

<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">
    <% data.courses.courses.each do |course| %>
      <div class="col-md-4">
        <div class="ibox">
          <div class="ibox-content product-box">
            <div class="product-imitation">
              <%= image_tag course.picture, resize_to: "260x400", :class => '', :alt => course.name %>
            </div>
            <div class="product-desc">
              <!-- <span class="product-price">
                $10
              </span> -->
              <small class="text-muted"><%= course.scope %></small>
              <a href="#" class="product-name"> <%= course.name %></a>
              <div class="small m-t-xs">
                <%= truncate(course.purpose, :length => 190) %>
              </div>
              <div class="m-t text-righ">
                <a href="#" class="btn btn-xs btn-outline btn-primary">Info <i class="fa fa-long-arrow-right"></i> </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
